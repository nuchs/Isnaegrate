syntax = "proto3";

option csharp_namespace = "Resin.Grpc";

import "Protos/common.proto"; 

message Request {
	EventType Stream = 1;
	int64 position = 2;
}
service Reader {
  rpc Read (Request) returns (IsgEvent);
} 
