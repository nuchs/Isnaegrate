syntax = "proto3";

option csharp_namespace = "Epoxy.Grpc";

import "Epoxy/isgevent.proto"; 

message WriteResponse {
}

service Writer {
  rpc Append (IsgEvent) returns (WriteResponse);
} 
